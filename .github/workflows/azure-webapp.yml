name: CI/CD - backend_streaming_video to Azure Web App

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

env:
  AZURE_WEBAPP_NAME: Git-Hub-API
  AZURE_WEBAPP_PACKAGE_PATH: '.'          
  NODE_VERSION: '20.x'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      # si NO agregaste "postinstall": "prisma generate" en package.json, deja este paso
      - name: Generate Prisma Client
        run: npx prisma generate

      # aquí podrías correr tests si tuvieras
      - name: Archive files
        run: |
          zip -r release.zip . -x "*.git*" "node_modules/*"

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: node-app
          path: release.zip

  deploy:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Download artifact
        uses: actions/download-artifact@v4
        with:
          name: node-app

      - name: Deploy to Azure Web App
        uses: azure/webapps-deploy@v3
        with:
          app-name: ${{ env.AZURE_WEBAPP_NAME }}
          publish-profile: <publishData><publishProfile profileName="Git-Hub-API - Web Deploy" publishMethod="MSDeploy" publishUrl="git-hub-api-fufgfgg8dyhxdsa6.scm.mexicocentral-01.azurewebsites.net:443" msdeploySite="Git-Hub-API" userName="REDACTED" userPWD="REDACTED" destinationAppUrl="https://git-hub-api-fufgfgg8dyhxdsa6.mexicocentral-01.azurewebsites.net" SQLServerDBConnectionString="REDACTED" mySQLDBConnectionString="" hostingProviderForumLink="" controlPanelLink="https://portal.azure.com" webSystem="WebSites"><databases /></publishProfile><publishProfile profileName="Git-Hub-API - FTP" publishMethod="FTP" publishUrl="ftp://waws-prod-mxc-003.ftp.azurewebsites.windows.net/site/wwwroot" ftpPassiveMode="True" userName="Git-Hub-API\$Git-Hub-API" userPWD="S8622yYrf4iFKoMSxvuLxMP7HiHwvB1c0Ncek6iuG05rblN3aiqNlN2loHZv" destinationAppUrl="https://git-hub-api-fufgfgg8dyhxdsa6.mexicocentral-01.azurewebsites.net" SQLServerDBConnectionString="" mySQLDBConnectionString="" hostingProviderForumLink="" controlPanelLink="https://portal.azure.com" webSystem="WebSites"><databases /></publishProfile><publishProfile profileName="Git-Hub-API - Zip Deploy" publishMethod="ZipDeploy" publishUrl="git-hub-api-fufgfgg8dyhxdsa6.scm.mexicocentral-01.azurewebsites.net:443" userName="REDACTED" userPWD="REDACTED" destinationAppUrl="https://git-hub-api-fufgfgg8dyhxdsa6.mexicocentral-01.azurewebsites.net" SQLServerDBConnectionString="REDACTED" mySQLDBConnectionString="" hostingProviderForumLink="" controlPanelLink="https://portal.azure.com" webSystem="WebSites"><databases /></publishProfile></publishData>
          package: release.zip
